{%- comment -%}
Icon text strip — v1.5.0
- v1.0.0 初始版本：支持 Color scheme + 动画开关 + 速度设置 + temp 占位文案
- v1.0.1 将 color_scheme 设置改为 select，以提高主题兼容性
- v1.0.3 使用内联 style 根据 color_scheme 设置背景/文字颜色
- v1.1.0 新增设置：Icon size、Space between icon and text
- v1.1.1 新增设置：Uppercase text（启用时将文本转为大写）
- v1.2.x 开始使用 type: color_scheme + 主题配色变量
- v1.4.0 新增 layout_mode：图标+文字（横向）、图+文（上下）、仅图标、仅文字
- v1.5.0 新增 section 上下左右 padding 设置
{%- endcomment -%}

{%- liquid
  assign layout_mode = section.settings.layout_mode | default: 'icon-text'

  assign padding_top = section.settings.padding_top | default: 20
  assign padding_bottom = section.settings.padding_bottom | default: 20
  assign padding_left = section.settings.padding_left | default: 16
  assign padding_right = section.settings.padding_right | default: 16
-%}

<section
  class="section section-benefits-marquee color-{{ section.settings.color_scheme }} benefits-marquee--{{ section.id }} benefits-marquee--layout-{{ layout_mode }}"
  style="
    padding-top: {{ padding_top }}px;
    padding-bottom: {{ padding_bottom }}px;
    padding-left: {{ padding_left }}px;
    padding-right: {{ padding_right }}px;
  "
>
  <div class="benefits-marquee__wrapper{% if section.settings.enable_animation %} benefits-marquee__wrapper--animated{% endif %}">
    <div class="benefits-marquee__track">
      {%- comment -%}
        为了实现无缝滚动，这里在同一条 track 里把内容重复两遍
      {%- endcomment -%}
      {% if section.blocks.size > 0 %}
        {% for n in (1..2) %}
          {% for block in section.blocks %}
            <div class="benefits-marquee__item" {{ block.shopify_attributes }}>
              {%- comment -%} 图标（在“仅文字”模式下不渲染） {%- endcomment -%}
              {% if layout_mode != 'text-only' and block.settings.icon != blank %}
                <div class="benefits-marquee__icon">
                  {{ block.settings.icon | image_url: width: 80 | image_tag: loading: 'lazy', alt: block.settings.text }}
                </div>
              {% endif %}

              {%- comment -%} 文本（在“仅图标”模式下不渲染） {%- endcomment -%}
              {% if layout_mode != 'icon-only' and block.settings.text != blank %}
                <span class="benefits-marquee__text">
                  {{ block.settings.text }}
                </span>
              {% endif %}
            </div>
          {% endfor %}
        {% endfor %}
      {% else %}
        {%- comment -%} 没有配置 block 时的占位示例文案（同样重复两遍） {%- endcomment -%}
        {% for n in (1..2) %}
          <div class="benefits-marquee__item">
            {% if layout_mode != 'text-only' %}
              <div class="benefits-marquee__icon"></div>
            {% endif %}
            {% if layout_mode != 'icon-only' %}
              <span class="benefits-marquee__text">Temp benefit #1</span>
            {% endif %}
          </div>
          <div class="benefits-marquee__item">
            {% if layout_mode != 'text-only' %}
              <div class="benefits-marquee__icon"></div>
            {% endif %}
            {% if layout_mode != 'icon-only' %}
              <span class="benefits-marquee__text">Temp benefit #2</span>
            {% endif %}
          </div>
          <div class="benefits-marquee__item">
            {% if layout_mode != 'text-only' %}
              <div class="benefits-marquee__icon"></div>
            {% endif %}
            {% if layout_mode != 'icon-only' %}
              <span class="benefits-marquee__text">Temp benefit #3</span>
            {% endif %}
          </div>
        {% endfor %}
      {% endif %}
    </div>
  </div>
</section>

{% style %}
.benefits-marquee--{{ section.id }} {
  /* 背景 & 字体颜色跟随当前 Color scheme */
  background-color: rgb(var(--color-background));
  color: rgb(var(--color-foreground));
}

/* 外层容器：只负责裁剪和宽度 */
.benefits-marquee--{{ section.id }} .benefits-marquee__wrapper {
  overflow: hidden;
  width: 100%;
}

/* 跑马灯轨道：一条 track，里边内容重复两遍 */
.benefits-marquee--{{ section.id }} .benefits-marquee__track {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: nowrap;
  width: max-content;
}

/* 单个卖点块：默认是横向“图标 + 文本” */
.benefits-marquee--{{ section.id }} .benefits-marquee__item {
  display: inline-flex;
  align-items: center;
  gap: {{ section.settings.icon_text_gap | default: 8 }}px;
  white-space: nowrap;
  font-size: 0.875rem;
  line-height: 1.4;
}

/* 图标尺寸：用设置控制宽度，高度自适应保持比例 */
.benefits-marquee--{{ section.id }} .benefits-marquee__icon img {
  display: block;
  width: {{ section.settings.icon_size | default: 36 }}px;
  height: auto;
  object-fit: contain;
  object-position: center;
}

/* 文本样式：可选大写 */
.benefits-marquee--{{ section.id }} .benefits-marquee__text {
  display: inline-block;
  {% if section.settings.uppercase_text %}
  text-transform: uppercase;
  {% endif %}
}

/* —— 布局模式：图 + 文 上下结构 —— */
.benefits-marquee--{{ section.id }}.benefits-marquee--layout-icon-text-vertical .benefits-marquee__item {
  flex-direction: column;
  align-items: center;
  text-align: center;
}

/* 布局模式：仅图标（让内容居中一点） */
.benefits-marquee--{{ section.id }}.benefits-marquee--layout-icon-only .benefits-marquee__item {
  justify-content: center;
}

/* 布局模式：仅文字（如需特殊样式可以在这里加）
.benefits-marquee--{{ section.id }}.benefits-marquee--layout-text-only .benefits-marquee__item {
}
*/

/* 滚动动画（移动端为主） */
.benefits-marquee--{{ section.id }} .benefits-marquee__wrapper--animated .benefits-marquee__track {
  animation-name: benefits-marquee-scroll-{{ section.id }};
  animation-timing-function: linear;
  animation-iteration-count: infinite;
  animation-duration: {% if section.settings.speed == 'slow' %}36s{% elsif section.settings.speed == 'fast' %}16s{% else %}24s{% endif %};
}

@keyframes benefits-marquee-scroll-{{ section.id }} {
  0% {
    transform: translateX(0);
  }
  100% {
    /* 内容在 track 中重复了两遍，这里往左移动 50% 的宽度，刚好无缝衔接 */
    transform: translateX(-50%);
  }
}

/* PC 端：不滚动，居中一行展示 */
@media (min-width: 768px) {
  .benefits-marquee--{{ section.id }} .benefits-marquee__wrapper {
    display: flex;
    justify-content: center;
  }

  .benefits-marquee--{{ section.id }} .benefits-marquee__wrapper--animated .benefits-marquee__track {
    animation: none;
  }

  .benefits-marquee--{{ section.id }} .benefits-marquee__track {
    transform: none;
  }

  .benefits-marquee--{{ section.id }} .benefits-marquee__item {
    font-size: 1rem;
  }
}
{% endstyle %}

{% schema %}
{
  "name": "Selling points marquee",
  "class": "section-benefits-marquee",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "layout_mode",
      "label": "Layout mode",
      "default": "icon-text",
      "options": [
        {
          "value": "icon-text",
          "label": "Icon + text (row)"
        },
        {
          "value": "icon-text-vertical",
          "label": "Image + text (stacked)"
        },
        {
          "value": "icon-only",
          "label": "Icon only"
        },
        {
          "value": "text-only",
          "label": "Text only"
        }
      ]
    },
    {
      "type": "checkbox",
      "id": "enable_animation",
      "label": "Enable scrolling animation (mobile)",
      "default": true
    },
    {
      "type": "select",
      "id": "speed",
      "label": "Scroll speed",
      "default": "medium",
      "options": [
        {
          "value": "slow",
          "label": "Slow"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "fast",
          "label": "Fast"
        }
      ]
    },
    {
      "type": "range",
      "id": "icon_size",
      "label": "Icon size (width)",
      "min": 16,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "icon_text_gap",
      "label": "Space between icon and text",
      "min": 0,
      "max": 32,
      "step": 2,
      "unit": "px",
      "default": 8
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_left",
      "label": "Padding left",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding_right",
      "label": "Padding right",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 16
    },
    {
      "type": "checkbox",
      "id": "uppercase_text",
      "label": "Uppercase text (convert text to all caps)",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Benefit item",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Temp benefit #1"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Icon text strip",
      "blocks": [
        {
          "type": "item",
          "settings": {
            "text": "Temp benefit #1"
          }
        },
        {
          "type": "item",
          "settings": {
            "text": "Temp benefit #2"
          }
        },
        {
          "type": "item",
          "settings": {
            "text": "Temp benefit #3"
          }
        }
      ]
    }
  ]
}
{% endschema %}
