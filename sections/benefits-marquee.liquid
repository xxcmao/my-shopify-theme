{%- comment -%}
Selling points marquee — v1.1.0
- v1.0.0 初始版本：Color scheme + 动画开关 + 速度设置 + temp 占位文案
- v1.0.1 将 color_scheme 改为 select 提升兼容
- v1.0.2 使用原生 color_scheme 类型（Scheme 1/2/3 UI）
- v1.1.0 应用主题常规外观结构：
  - color-{{ section.settings.color_scheme }} + gradient 外壳
  - page-width 宽度容器
  - padding_top / padding_bottom 动态上下留白
{%- endcomment -%}

<section class="section section-benefits-marquee color-{{ section.settings.color_scheme }} gradient">
  <div class="page-width section-{{ section.id }}-padding benefits-marquee--{{ section.id }}">
    <div class="benefits-marquee__wrapper{% if section.settings.enable_animation %} benefits-marquee__wrapper--animated{% endif %}">
      <div class="benefits-marquee__track">
        {%- comment -%}
          为了实现无缝滚动，这里在同一条 track 里把内容重复两遍
        {%- endcomment -%}
        {% if section.blocks.size > 0 %}
          {% for n in (1..2) %}
            {% for block in section.blocks %}
              <div class="benefits-marquee__item" {{ block.shopify_attributes }}>
                {% if block.settings.icon != blank %}
                  <div class="benefits-marquee__icon">
                    {{ block.settings.icon | image_url: width: 80 | image_tag: loading: 'lazy', alt: block.settings.text }}
                  </div>
                {% endif %}

                {% if block.settings.text != blank %}
                  <span class="benefits-marquee__text">
                    {{ block.settings.text }}
                  </span>
                {% endif %}
              </div>
            {% endfor %}
          {% endfor %}
        {% else %}
          {%- comment -%} 没有配置 block 时的占位示例文案（同样重复两遍） {%- endcomment -%}
          {% for n in (1..2) %}
            <div class="benefits-marquee__item">
              <span class="benefits-marquee__text">Temp benefit #1</span>
            </div>
            <div class="benefits-marquee__item">
              <span class="benefits-marquee__text">Temp benefit #2</span>
            </div>
            <div class="benefits-marquee__item">
              <span class="benefits-marquee__text">Temp benefit #3</span>
            </div>
          {% endfor %}
        {% endif %}
      </div>
    </div>
  </div>
</section>

{% style %}
/* 按主题习惯，用 padding_top / padding_bottom 控制上下留白 */
.section-{{ section.id }}-padding {
  padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
  padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
}

@media screen and (min-width: 750px) {
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
}

/* 外层容器：只负责裁剪和宽度 */
.benefits-marquee--{{ section.id }} .benefits-marquee__wrapper {
  overflow: hidden;
  width: 100%;
}

/* 跑马灯轨道：一条 track，里边内容重复两遍 */
.benefits-marquee--{{ section.id }} .benefits-marquee__track {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: nowrap;
  width: max-content;
}

.benefits-marquee--{{ section.id }} .benefits-marquee__item {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  white-space: nowrap;
  font-size: 0.875rem;
  line-height: 1.4;
}

.benefits-marquee--{{ section.id }} .benefits-marquee__icon img {
  display: block;
  width: 37px;
  height: 43px;
  object-fit: contain;
  object-position: center;
}

.benefits-marquee--{{ section.id }} .benefits-marquee__text {
  display: inline-block;
}

/* 滚动动画（移动端为主） */
.benefits-marquee--{{ section.id }} .benefits-marquee__wrapper--animated .benefits-marquee__track {
  animation-name: benefits-marquee-scroll-{{ section.id }};
  animation-timing-function: linear;
  animation-iteration-count: infinite;
  animation-duration: {% if section.settings.speed == 'slow' %}36s{% elsif section.settings.speed == 'fast' %}16s{% else %}24s{% endif %};
}

@keyframes benefits-marquee-scroll-{{ section.id }} {
  0% {
    transform: translateX(0);
  }
  100% {
    /* 内容在 track 中重复了两遍，这里往左移动 50% 的宽度，刚好无缝衔接 */
    transform: translateX(-50%);
  }
}

/* PC 端：不滚动，居中一行展示 */
@media (min-width: 768px) {
  .benefits-marquee--{{ section.id }} .benefits-marquee__wrapper {
    display: flex;
    justify-content: center;
  }

  .benefits-marquee--{{ section.id }} .benefits-marquee__wrapper--animated .benefits-marquee__track {
    animation: none;
  }

  .benefits-marquee--{{ section.id }} .benefits-marquee__track {
    transform: none;
  }

  .benefits-marquee--{{ section.id }} .benefits-marquee__item {
    font-size: 1rem;
  }
}
{% endstyle %}

{% schema %}
{
  "name": "Selling points marquee",
  "class": "section-benefits-marquee",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 36
    },
    {
      "type": "checkbox",
      "id": "enable_animation",
      "label": "Enable scrolling animation (mobile)",
      "default": true
    },
    {
      "type": "select",
      "id": "speed",
      "label": "Scroll speed",
      "default": "medium",
      "options": [
        {
          "value": "slow",
          "label": "Slow"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "fast",
          "label": "Fast"
        }
      ]
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Benefit item",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Temp benefit #1"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Selling points marquee",
      "blocks": [
        {
          "type": "item",
          "settings": {
            "text": "Temp benefit #1"
          }
        },
        {
          "type": "item",
          "settings": {
            "text": "Temp benefit #2"
          }
        },
        {
          "type": "item",
          "settings": {
            "text": "Temp benefit #3"
          }
        }
      ]
    }
  ]
}
{% endschema %}
